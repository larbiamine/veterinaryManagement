model Vet {
  id          Int      @id @default(autoincrement())
  firstName   String
  lastName    String
  phoneNumber String
  email       String  @default("defaultVet@email.com")
  city        String
  address     String
  animals     Animal[]
}